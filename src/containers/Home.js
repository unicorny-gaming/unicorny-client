import Countdown from "components/common/Index/Countdown";

// Home page component
export default class Home extends React.Component {
    // render
    render() {
        return (
          <Countdown/>
        );
    }
}
